# Yo Mama Bot - Requirements
# Python 3.10+

# Core dependencies
# Shared LLM layer (Gemini + Ollama with guardrails and failover) from the
# hypeman-social library used across ChiefGyk3D's daemons. The extras pull
# google-genai and ollama.
hypeman-social[gemini,ollama]>=0.2.0,<0.3
# GHSA-mf9w-mj56-hr94: set_key followed symlinks, allowing arbitrary file
# overwrite via cross-device rename. Fixed in 1.2.2; 1.2.3 is current.
python-dotenv==1.2.3
doppler-sdk==1.3.0

# Platform integrations - Discord and Matrix
discord.py==2.6.4
matrix-nio==0.25.2

# Optional: Additional platforms
# atproto==0.0.63  # Bluesky
# Mastodon.py==2.1.4  # Mastodon

# Optional: Additional secret managers
# boto3>=1.26.0  # For AWS Secrets Manager
# hvac>=1.2.0    # For HashiCorp Vault

# Testing dependencies
# GHSA-6w46-j5rx-g56g: vulnerable tmpdir handling. Fixed in 9.0.3, which is
# the patch line for 9.0.x; 9.1.x is a feature bump and stays out of scope here.
pytest==9.0.3
pytest-asyncio==1.3.0
