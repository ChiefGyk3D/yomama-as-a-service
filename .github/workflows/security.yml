# CodeQL, gitleaks, pip-audit, dependency review on pull requests, and Snyk
# (token from Doppler). The jobs live in ChiefGyk3D/git-your-ship-together.
name: Security

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    # Weekly, Monday 06:00 UTC, so new advisories surface between commits
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    uses: ChiefGyk3D/git-your-ship-together/.github/workflows/security.yml@5df66539de085aff73dcf1f2957615689b846298 # v1.2.1
    permissions:
      contents: read
      security-events: write
      pull-requests: write
      id-token: write
    secrets:
      DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}   # optional Service Token fallback; unset means OIDC only
    with:
      codeql-queries: security-extended,security-and-quality
      # Was .github/codeql/codeql-config.yml. yo_mama/secrets.py exists to
      # load and return secrets, so the two clear-text alerts are its purpose,
      # not a finding.
      codeql-config: |
        query-filters:
          - exclude:
              id: py/clear-text-logging-sensitive-data
              paths:
                - "**/yo_mama/secrets.py"
                - "yo_mama/secrets.py"
                - "/yo_mama/secrets.py"
          - exclude:
              id: py/clear-text-storage-sensitive-data
              paths:
                - "**/yo_mama/secrets.py"
                - "yo_mama/secrets.py"
                - "/yo_mama/secrets.py"
      pip-audit-requirements: requirements.txt
      snyk: true
      egress-policy: block
      doppler-project: ci
      doppler-config: ci
      doppler-identity-id: ${{ vars.DOPPLER_IDENTITY_ID }}
